%h1 Listing lessons

%table
  %tr
    %th
    %th
    %th

  - @lessons.each do |lesson|
    %tr
      %td= link_to 'Show', course_section_lesson_path(@course, @section, lesson)
      %td= link_to 'Edit', edit_course_section_lesson_path(@course, @section, lesson)
      %td= link_to 'Destroy', lesson, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Lesson', new_lesson_path
