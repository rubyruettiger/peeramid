%h1 Listing sections

%table
  %tr
    %th Name
    %th Description
    %th
    %th
    %th

  - @sections.each do |section|
    %tr
      %td= section.name
      %td= section.description
      %td= link_to 'Show', course_section_path(@course, section)
      %td= link_to 'Edit', edit_course_section_path(@course,section)

%br

= link_to 'New Section', new_course_section_path(@course)
